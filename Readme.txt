train and test folders are expected to be in the same folder as the digit_gmm.py file
train folder should contain all the training wav files
test folder should contain all the test wav files
change the w_s (window size) and hop_length in lines 15 and 16 if required
change the top_db value in line 32 for training files and in line 115 for testing files if required
(currently tuned for digit wav files of 194102309 as test files and others' digit wav files as training)